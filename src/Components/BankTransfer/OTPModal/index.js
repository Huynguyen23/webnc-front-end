import { connect } from 'react-redux';
import JobModal from './OTPModal';


const mapStateToProps = () => ({});

const mapDispatchToProps = dispatch => {
  return {
    
  };
};
export default connect(mapStateToProps, mapDispatchToProps)(JobModal);
